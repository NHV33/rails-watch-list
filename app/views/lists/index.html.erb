<div class="container w-50 d-flex flex-column justify-content-center align-content-center">
  <% @lists.each do |list| %>
    <div class="card m-1 d-flex flex-row">
      <%# <img class="card-img-top" src="..." alt="Card image cap"> %>
        <%= cl_image_tag list.photo.key, width: 100, height: 100, crop: :fill %>
        <div class="card-body">
          <h5 class="card-title"><%= list.name %></h5>
          <%= link_to "View List", list_path(list), class: "btn btn-primary" %>
          <%# link_to "Delete List", list_path(list), class: "btn btn-danger text-white" %>
        </div>
    </div>
  <% end %>
  <%= link_to '✚ Create New List', new_list_url, class: "btn btn-primary" %>
</div>
